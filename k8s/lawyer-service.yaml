apiVersion: apps/v1
kind: Deployment
metadata:
  name: lawyer-service
  namespace: lawbridge
spec:
  replicas: 2
  selector:
    matchLabels: { app: lawyer-service }
  template:
    metadata:
      labels: { app: lawyer-service }
    spec:
      containers:
      - name: lawyer-service
        image: lawbridge/lawyer-service:latest
        ports:
        - containerPort: 8003
        env:
        - name: DB_HOST
          value: lawyer-db.lawbridge.svc.cluster.local
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          valueFrom:
            secretKeyRef: { name: lawbridge-db-secrets, key: LAWYER_DB }
        - name: DB_USER
          valueFrom:
            secretKeyRef: { name: lawbridge-db-secrets, key: DB_USER }
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef: { name: lawbridge-db-secrets, key: DB_PASSWORD }
        envFrom:
        - secretRef:
            name: lawbridge-secrets
        readinessProbe:
          httpGet:
            path: /api/v1/health/
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/v1/health/
            port: 8003
          initialDelaySeconds: 20
          periodSeconds: 20
        resources:
          requests: { cpu: 100m, memory: 128Mi }
          limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: lawyer-service
  namespace: lawbridge
spec:
  selector: { app: lawyer-service }
  ports:
  - port: 80
    targetPort: 8003
